/* this makes it so i can set my own margins and padding */
* {
    margin: 0;
    padding: 0;
}

/* custom font */
@font-face {
    font-family: CrozzoFont;
    src: url("Fonts/solidsans/SolidSans.ttf");
}

/* website body and layout */
body {
    background-color: #282828;
    height: 800px;

    display: grid;
    grid-template-columns: 70% 30%;
    grid-template-rows: 10% 90%;
}

/* the navigation bar */
nav {
    background-color: white;
    grid-column: 1/3;
    grid-row: 1/2;
    width: 100%;
    height: 100%;
    margin: 5px 0px;

    display: grid;
    grid-template-columns: 1fr 2fr 1fr;
    grid-template-rows: 1fr;
}

/* logo styles */
.logo {
    width: 8vw;
    height: auto;
    justify-self: center;
}

/* for the links in the nav */
ul {
    list-style-type: none;
    margin: 2% 0px;
    padding: 0;
    grid-column: 2/3;
}

/* for the links in the nav */
li {
    float: left;
}

/* for the links in the nav */
li a {
    display: inline;
    font-family: CrozzoFont;
    font-size: 3vw;
    color: red;
    padding: 14px 16px;
    text-decoration: none;
}

/* makes it so nav links become highlighted by cursor to tell which one the user hovered over */
li a:hover {
    color: white;
    background-color: red;
    border-radius: 20px;
}

/* layout and styles of the first box on the home page */
.homebox1 {
    grid-column: 1/2;
    grid-row: 2/3;
    background-color: white;
    margin: 10px;
}

/* the ad on the home page */
.ad {
    width: 100%;
    height: 100%;
}

/* layout and styles of the second box on the home page*/
.homebox2 {
    grid-column: 2/3;
    grid-row: 2/3;
    background-color: white;
    border: 5px solid red;
    border-radius: 10px;
    margin: 10px;
}

/* layout and styles of the first box on the shop page */
.shopbox1 {
    grid-column: 1/2;
    grid-row: 2/3;
    background-color: white;
    border: 5px solid red;
    border-radius: 10px;
    margin: 10px;

    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    grid-template-rows: 10% 90%;
}

/* overall styles of the products on the shop page */
.shopbox1 img {
    width: 90%;
    justify-self: center;
    border: 5px solid red;
    margin: 5px;
}

/* the header of the shop page */
.shopbox1 h1 {
    grid-column: 2/3;
}

/* specfic layout of the products on the shop page */
.product1 {
    grid-column: 1/2;
    background-color: #282828;
    margin: 5px;
}

/* specfic layout of the products on the shop page */
.product2 {
    grid-column: 2/3;
    background-color: #282828;
    margin: 5px;
}

/* specfic layout of the products on the shop page */
.product3 {
    grid-column: 3/4;
    background-color: #282828;
    margin: 5px;
}

/* layout and styles of the second box on the shop page */
.shopbox2 {
    grid-column: 2/3;
    grid-row: 2/3;
    background-color: white;
    margin: 10px;
}

/* slideshow image */
#img {
    width: 100%;
}

/* styles of the button for the slide show */
button {
    font-family: CrozzoFont;
    font-size: 2vw;
    color: red;

    background-color: white;
    border: 5px solid red;
    border-radius: 5px;
    margin: 5px;
}

/* makes it so the user can tell they are using the button */
button:hover {
    color: white;
    background-color: red;
}

/* ad on the contact page */
.ad2 {
    width: 100%;
}

/* layout and styles for the first box on the contact page */
.contactbox1 {
    grid-column: 1/2;
    grid-row: 2/3;
    background-color: white;
    border: 5px solid red;
    border-radius: 10px;
    margin: 10px;
}

/* layout and styles for the second box on the contact page */
.contactbox2 {
    grid-column: 2/3;
    grid-row: 2/3;
    background-color: white;
    margin: 10px;
}

/* layout and styles for the boxes with contact info on the contact/about page */
.contactcontainer1 {
    background-color: #282828;
    margin: 5px;
    border: 5px solid red;
}

/* layout and styles for the boxes with contact info on the contact/about page */
.contactcontainer2 {
    background-color: #282828;
    margin: 5px;
    border: 5px solid red;
}

/* layout and styles for the boxes with contact info on the contact/about page */
.contactcontainer3 {
    background-color: #282828;
    margin: 5px;
    border: 5px solid red;
}

/* layout and styles for text with the p tag */
p {
    color: red;
    font-family: CrozzoFont;
    font-size: 2vw;
    text-align: center;
}

/* layout and styles for text with the h1 tag */
h1 {
    color: red;
    font-family: CrozzoFont;
    font-size: 4vw;
    text-align: center;
    text-decoration: underline;
}
